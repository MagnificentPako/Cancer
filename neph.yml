zir:
  command:
    zir run

clean:
  command: |
    rm -f src/*.emojib
    rm -f src/main.emojib

build:
  command:
    emojicodec src/main.emojic

main:
  depends_on:
    - clean
    - zir
    - build